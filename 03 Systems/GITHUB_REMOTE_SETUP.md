# GitHub Remote Setup

## Current state
- Local Git repo initialized for the Obsidian vault
- Default branch should be `main`
- First commit should exist after initialization

## Next step
Create a GitHub repo, then connect it:

```bash
cd /Users/gibby.ai/.openclaw/workspace/obsidian
git remote add origin <your-repo-url>
git push -u origin main
```

## Suggested repo purpose
A private shared-memory repo for Ben + agents.

## Suggested naming
- `gibby-obsidian`
- `shared-memory-vault`
- `ben-ops-vault`

## Recommendation
Make it private.
